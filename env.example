# PostgreSQL 接続設定（Django: config/settings.py が `env` または `.env` を読み込みます）
# 使い方:
# 1) このファイルを `env` にコピー（または `.env` を作成）
# 2) 値を自分の環境に合わせて編集

# PostgreSQLを使う場合は 1 を設定（未設定/0の場合はSQLiteを使用）
USE_POSTGRES=1

POSTGRES_DB=data_aggregation
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432

